import { styled } from '@mui/material/styles'
import Typography from '@mui/material/Typography'

const FooterOneTypography = styled(Typography)(({ theme }) => ({
  color: theme.palette.grey[800],
}))

export default FooterOneTypography
